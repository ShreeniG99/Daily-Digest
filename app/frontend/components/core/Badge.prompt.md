Kind badge — labels a card's content kind with an icon + warm hue.

```jsx
<Badge kind="paper" />
<Badge kind="youtube" solid />
```

`kind`: news | youtube | paper | repo | opportunity. `solid` fills with the hue; otherwise tinted. Auto-sets icon and label.
